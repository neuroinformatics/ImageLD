# ImageJ でのメニューリストです．
# 書き方は見れば大体わかると思います．
#
# ij.plugin.PlugIn の run() は String を引数にとるので，
# 引数を指定するときはダブルクオテーション "" を忘れないようにして下さい．
# 引数不要の場合は括弧を省略できます．
#
# 区切りにタブを使ったり行の末尾にスペースが入ってたりすると，そのプログラムが起動しなくなるので注意．

Plugins, "About Behavior", behavior.plugin.AboutBehavior

Plugins>Light/Dark Transition, "LD Online (1 chamber)", behavior.plugin.LDOnline("1")
Plugins>Light/Dark Transition, "LD Online (2 chambers)", behavior.plugin.LDOnline("2")
Plugins>Light/Dark Transition, "LD Online (4 chambers)", behavior.plugin.LDOnline("4")
Plugins>Light/Dark Transition, "LD Offline", behavior.plugin.LDOffline
